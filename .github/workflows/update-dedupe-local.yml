name: Update Dedupe Skill (Local Disabled)
on:
  workflow_dispatch:
    inputs:
      lookback_days:
        description: Number of days to look back for closed-as-duplicate signals
        required: false
        default: '7'
        type: string
jobs:
  update_dedupe:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: |
          echo "The upstream reusable workflow warpdotdev/oz-for-oss/.github/workflows/update-dedupe.yml@main is not available."
          echo "This local workflow is disabled until an available replacement workflow is published."
